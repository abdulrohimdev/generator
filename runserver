#!/usr/bin/php
<?php
   require_once(__DIR__."./env/Server.php");
   use env\Server;

   $server = new Server();
   $server->host ="localhost"; //setting your server
   $server->port =3000; //setting your port
   $server->run();

?> 